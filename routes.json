{
  "comment": "Route configuration for API Gateway. Set 'mock' to use Prism mock server, 'real' to use actual backend.",
  "GET /brands": "real",
  "POST /brands": "real",
  "GET /brands/{id}": "real",
  "POST /evaluate": "real",
  "GET /brands/{id}/rules": "real",
  "POST /brands/{id}/rules": "real",
  "PUT /brands/{id}/rules": "real",
  "GET /brands/rules": "real",
  "POST /evaluations/{id}/fix": "real",
  "GET /evaluations/{id}/results": "real"
}

